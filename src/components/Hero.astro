---
import heroImage from "@/assets/hero-pets.jpg";

const bgStyle = `
  background-image: url('${heroImage.src}');
  background-size: cover;
  background-position: center;
  opacity: 0.15;
`;
---
<section class="relative min-h-[600px] flex items-center overflow-hidden">
  <!-- Background Image Layer -->
  <div class="absolute inset-0" style={bgStyle}></div>
  <!-- Optional gradient overlay (kept from original intent) -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary/10 via-accent/10 to-secondary/10 pointer-events-none"></div>

  <div class="container mx-auto px-4 py-20 relative z-10">
    <div class="max-w-3xl mx-auto text-center space-y-8">
      <h1 class="text-5xl md:text-7xl font-bold leading-tight">
        Encuentra Tu
        <span class="block text-primary">Compañero Ideal</span>
      </h1>
      <p class="text-xl md:text-2xl text-muted-foreground">
        Cada perro y gato merece un hogar que le brinde amor. Busca entre nuestros cariñosos adoptables que esperan conocer a su familia.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/adoptables" class="rounded-full text-lg px-8 h-14 shadow-lg hover:shadow-xl transition-shadow inline-flex items-center justify-center bg-primary text-primary-foreground focus:outline-none focus:ring-2 focus:ring-primary/50">
          Ver Adoptables
        </a>
        <a href="/como-funciona" class="rounded-full text-lg px-8 h-14 border-2 hover:bg-muted inline-flex items-center justify-center border-input text-foreground focus:outline-none focus:ring-2 focus:ring-ring/30">
          ¿Cómo funciona?
        </a>
      </div>
    </div>
  </div>
</section>
